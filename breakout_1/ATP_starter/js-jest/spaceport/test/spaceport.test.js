//const {Shop, Item} = require("../src/gilded_rose");

describe("Spaceport Launch Listings", function() {

  // TODO - Use the Stub Recipe to test that launches are sorted correctly
  it("are ordered by Destination when Destinations are unique", () => {
    // Step 1. Create a LaunchInfoProviderStub

    // Step 2 & 3 & 4.
    // Create the SUT - SpaceportDepartureBoard, using Contructor Injection
    // to connect with the LaunchInfoProvider
    // The constructor will request a list of (unordered) LaunchInfo from the Provider

    // Step 5. Verify the launches are sorted correctly
    throw new Error("TODO - Write a test for the LaunchInfo sorting behavior (and implement that sorting behavior)")
  });
});
